{
  "name": "partner-portal",
  "version": "0.1.0",
  "private": true,
  "description": "Recast Software Partner Portal — GitHub Pages static site. The package.json exists only for devDependencies used by local scripts (skill upload, tests). The shipped site has no build step.",
  "type": "module",
  "scripts": {
    "upload-map-skill": "node scripts/upload_recast_map_skill.mjs",
    "test": "node --test 'tests/*.test.mjs'"
  },
  "devDependencies": {
    "@anthropic-ai/sdk": "^0.65.0"
  }
}
