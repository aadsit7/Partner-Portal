{
  "name": "partner-portal",
  "version": "0.1.0",
  "private": true,
  "description": "Recast Software Partner Portal — GitHub Pages static site. The package.json exists only for devDependencies used by local scripts and tests. The shipped site has no build step.",
  "type": "module",
  "scripts": {
    "test": "node --test 'tests/*.test.mjs'"
  }
}
